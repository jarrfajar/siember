import 'package:flutter/material.dart';

ThemeData themaLight = ThemeData(
  brightness: Brightness.light,
  // primaryColor: Color(0xff9345f2),
);

ThemeData themaDark = ThemeData(
  brightness: Brightness.dark,
  // primaryColor: Color(0xff9345f2),
);
